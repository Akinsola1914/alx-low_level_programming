#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	int A;

	for (A = '0'; A <= '9'; A++)
		putchar(A);
	putchar('\n');
	return (0);
}
